import React from "react";
import FormBuilder from "../components/FormBuilder";

const Home: React.FC = () => {
    return (
        <div>
            <FormBuilder />
        </div>
    );
};

export default Home;
